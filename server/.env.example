# Copy to .env and fill in. Run from repo root: npm run api (or cd server && npm start)

# Required: Google Gemini API key (get from https://aistudio.google.com/apikey)
GEMINI_API_KEY=your-gemini-api-key

# Required for AI-generated images (inventory/product): Imagine Pro API key (Bearer token from https://www.imaginepro.ai)
# Each image generation uses 1 credit; QR codes are generated locally and do not use Imagine Pro.
IMAGINEPRO_API_KEY=your-imaginepro-bearer-token

# Optional: API server port (default 3003)
PORT=3003

# Optional: Ethereum / Alchemy (wallet verification, blockchain)
# ETHEREUM_MAINNET_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY
# ALCHEMY_NFT_API_KEY=your-alchemy-key
# GAS_POLICY_ID=your-gas-policy-id   # if using Alchemy for gas-sponsored txs

# Optional: Gemini model (default gemini-2.5-flash). Free tier has rate limits.
# GEMINI_MODEL=gemini-2.5-flash
